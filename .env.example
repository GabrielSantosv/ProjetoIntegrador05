# Este arquivo não é usado diretamente pelo sistema.
# Configure as variáveis de ambiente copiando os arquivos abaixo:
#
#   backend/.env.example  →  backend/.env    (banco de dados, OCR, paths)
#   frontend/.env.example →  frontend/.env   (URL da API)
